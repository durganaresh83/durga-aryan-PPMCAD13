# Cluster name
cluster.name: my-elk-cluster

# Node name
node.name: node-1

# Network settings (allow external access)
network.host: 0.0.0.0
transport.host: 0.0.0.0

# Discovery settings
discovery.type: single-node